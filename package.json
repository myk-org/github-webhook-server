{
  "name": "github-webhook-server",
  "version": "1.0.0",
  "description": "ESLint configuration for GitHub webhook server frontend",
  "private": true,
  "devDependencies": {
    "eslint": "^9.0.0",
    "globals": "^15.0.0"
  },
  "scripts": {
    "lint": "eslint ."
  }
}
